Restaurant Page
This is a restaurant page project created using JavaScript and Webpack. The project is designed to showcase the use of Webpack for module bundling and JavaScript for dynamic content loading.

Installation
To install the project, follow these steps:

Clone the repository to your local machine using git clone https://github.com/your-username/restaurant-page.git.
Navigate to the project directory using cd restaurant-page.
Install the dependencies using npm install.
Usage
To run the project, use the following command:

This will start a local development server and open the restaurant page in your default web browser.

Features
The restaurant page includes the following features:

A navigation menu with links to different sections of the page.
A menu section that displays different food items with images and descriptions.
A contact section that displays the restaurant's contact information.